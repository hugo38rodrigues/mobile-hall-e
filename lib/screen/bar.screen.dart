import 'package:flutter/material.dart';

class BarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Page des Bars", style: TextStyle(fontSize: 24)),
    );
  }
}
